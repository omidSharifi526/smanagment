import { GetServerSideProps } from "next";
import Cookies from "js-cookie";

export default function UserDashboard() {
  // const token = req.cookies.get("token")?.value;
    return <h1>🎯 داشبورد کاربران عادی</h1>;
  }
  